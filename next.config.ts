import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["gastronomiq-vinotique-bucket-eu-central-1.s3.amazonaws.com"],
  },
};

export default nextConfig;
